function square(number: number) {
return number * number;
}

console.log(square(4)); 
console.log(square(9)); 

function minutesToSeconds(minutes: number) {
return minutes * 60;
}

console.log(minutesToSeconds(2));
console.log(minutesToSeconds(90));

function buildFullName(firstname: string, lastname: string, title: string) {
return title + " " + firstname + " " + lastname;
}

console.log(buildFullName("Ada", "Lovelace", "Ms.")); 

console.log(buildFullName("John", "Doe", "Dr.")); 

function canVote(age: number) {
if (age >= 18) {
return "Yes, you can vote"
} else {
    return "Not old enough" 
}
}
console.log(canVote(18));



function applyDiscount(orginalprice: number discountpercent: number) {
let Finalprice = orginalprice * (1 - discountpercent/100);
return finalPrice;

}

function FizzBuzz(n) {
if (n % 3 === 0 && n % 5 === 0) {
return "FizzBuzz";
} else if (n % 3 === 0)
return "Fizz"
}  else if (n % 5 === 0) {
return "Buzz"
} else {
    return n;
}



//Function is a code that preforms a speific task told to do
//Prevents having to write the same logic multiple times and makes code look better
//parameter is a named varible listed in the function (a placeholder), arguement is a value you pass. 
